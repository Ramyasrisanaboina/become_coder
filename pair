#include<bits/stdc++.h>
using namespace std;
int main()
{
	pair<int,int>p={1,13};
	cout<<p.first<<"  "<<p.second<<endl;
	//creating array with vector and pair
	vector<pair<int,int>>q;
	q.push_back({10,23});
	q.push_back({12,234});
	for(int i=0;i<q.size();i++){
		cout<<q[i].first<<" "<<q[i].second<<endl;
	}
}
